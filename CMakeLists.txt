cmake_minimum_required(VERSION 3.25)



## Default target
if("${TARGET}" STREQUAL "STM32H730")
    set(TARGET "STM32H730" CACHE STRING "Build target")
    set(MCU_FAMILY STM32H7)
    set(MCU_MODEL STM32H730)
endif()

if ("${TARGET}" STREQUAL "STM32H753")
    set(TARGET "STM32H753" CACHE STRING "Build target")
    set(MCU_FAMILY STM32H7)
    set(MCU_MODEL STM32H753ZI)
endif()

if("${TARGET}" STREQUAL "Native")
    set(CMAKE_ASM_FLAGS "${CMAKE_C_FLAGS} -x assembler-with-cpp -MMD -MP")
    set(CMAKE_C_FLAGS 
        "${CMAKE_C_FLAGS} -Wall -fdata-sections -ffunction-sections"
    )

    set(CMAKE_C_FLAGS_DEBUG     "-O0 -g3")
    set(CMAKE_C_FLAGS_RELEASE   "-Os -g0")
    set(CMAKE_CXX_FLAGS_DEBUG   "-O0 -g3")
    set(CMAKE_CXX_FLAGS_RELEASE "-Os -g0")

    set(CMAKE_CXX_FLAGS 
        "${CMAKE_CXX_FLAGS} -fno-rtti -fno-exceptions -fno-threadsafe-statics"
    )
    set(TOOLCHAIN_LINK_LIBRARIES "${TOOLCHAIN_LINK_LIBRARIES} m pthread rt")
else()
    set(CMAKE_TOOLCHAIN_FILE
        ${CMAKE_CURRENT_SOURCE_DIR}/toolchain/arm-none-eabi.cmake
        CACHE FILEPATH "toolchain file"
        FORCE
    )
endif()

project(copperhead C CXX ASM)

include_directories(${CMAKE_CURRENT_SOURCE_DIR}/api)

# must come after `project()`
string(APPEND CMAKE_EXE_LINKER_FLAGS " -Wl,-Map=${PROJECT_NAME}.map")

## Default build type
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE "Debug")
endif()

message("Build type: " ${CMAKE_BUILD_TYPE})
message("Target: " ${TARGET})

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_C_STANDARD 17)
set(CMAKE_EXPORT_COMPILE_COMMANDS true)

add_subdirectory(ext)
add_subdirectory(applications)
if(NOT "${TARGET}" STREQUAL "Native")
    add_subdirectory(drivers)
    add_subdirectory(hal)
else()
    add_subdirectory(simulations)
endif()

# Do something so that the user knows they need openocd installed
find_program(OPENOCD openocd)
if(OPENOCD)
    add_custom_target(flash
        COMMAND ${OPENOCD} -f ${CMAKE_SOURCE_DIR}/openocd.cfg -c "program $<TARGET_FILE:example> verify reset exit"
        DEPENDS example 
        COMMENT "Flashing target hardware"
    )
else()
    message(WARNING "OpenOCD not found! Install OpenOCD to use 'flash' target.")

endif()
